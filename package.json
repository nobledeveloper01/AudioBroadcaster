{
  "name": "live-audio-prod",
  "version": "1.0.0",
  "description": "Production-ready minimal live audio app (WebSocket relay + server-side recording)",
  "main": "server/server.js",
  "scripts": {
    "dev": "cross-env NODE_ENV=development node server/server.js",
    "start": "cross-env NODE_ENV=production node server/server.js"
  },
  "dependencies": {
    "compression": "^1.7.4",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-rate-limit": "^6.8.0",
    "helmet": "^7.0.0",
    "tailwindcss": "^2.2.19",
    "uuid": "^9.0.0",
    "ws": "^8.13.0"
  },
  "devDependencies": {
    "cross-env": "^10.1.0"
  }
}
